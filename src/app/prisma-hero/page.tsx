import { PrismaHero } from '@/components/ui/prisma-hero';

export default function PrismaHeroDemoPage() {
  return (
    <div className="min-h-screen bg-[#111014]">
      <PrismaHero />
    </div>
  );
}
